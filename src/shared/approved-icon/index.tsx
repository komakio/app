import React, { memo } from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

export const ApprovedIcon = memo(() => (
  <Icon name="check-circle" size={24} color="#3BE338" />
));
